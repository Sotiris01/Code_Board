// Άσκηση: Αντιμετάθεση Μεταβλητών (Swap)
// Αντάλλαξε τις τιμές δύο μεταβλητών

#include <iostream>
using namespace std;

int main() {
    int a, b;
    
    cout << "Δώσε τον πρώτο αριθμό: ";
    cin >> a;
    cout << "Δώσε τον δεύτερο αριθμό: ";
    cin >> b;
    
    cout << "Πριν: a=" << a << ", b=" << b << endl;
    
    // TODO: Αντιμετάθεση με βοηθητική μεταβλητή
    // Βήμα 1: Αποθήκευσε το a σε temp
    int temp = __________;
    
    // Βήμα 2: Βάλε το b στο a
    a = __________;
    
    // Βήμα 3: Βάλε το temp στο b
    b = __________;
    
    cout << "Μετά: a=" << a << ", b=" << b << endl;
    
    return 0;
}
