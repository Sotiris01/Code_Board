// Άσκηση: Περίφραξη Χωραφιού (Άσκηση 3 από PDF)
// Υπολόγισε το μήκος φράχτη για χωράφι σχήματος Π

#include <iostream>
using namespace std;

int main() {
    double x, y;
    
    /*
        Σχήμα χωραφιού (Π):
        
        +---x---+
        |       |
        x       x
        |       |
        +       +
            y
        
        Περίμετρος = x + x + x + y = 3x + y
    */
    
    // Διάβασε τις διαστάσεις
    cout << "Δώσε την πλευρά x: ";
    cin >> x;
    cout << "Δώσε την πλευρά y: ";
    cin >> y;
    
    // Υπολόγισε την περίμετρο (3*x + y)
    double perimeter = 3 * x + y;
    
    cout << "\nΜήκος φράχτη που χρειάζεται: " << perimeter << " μέτρα" << endl;
    
    return 0;
}
