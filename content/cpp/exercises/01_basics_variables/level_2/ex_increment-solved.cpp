// Άσκηση: Προσαύξηση και Μείωση (++x vs x++)
// Δείξε τη διαφορά μεταξύ πρότερης και ύστερης προσαύξησης

#include <iostream>
using namespace std;

int main() {
    int x = 5;
    int y = 5;
    
    // Εμφάνισε την αρχική τιμή του x
    cout << "Αρχική τιμή x = " << x << endl;
    
    // Ύστερη προσαύξηση (post-increment): πρώτα χρησιμοποιεί, μετά αυξάνει
    cout << "Ύστερη προσαύξηση x++: " << x++ << endl;
    cout << "Τιμή x μετά: " << x << endl;
    
    cout << endl;
    
    // Εμφάνισε την αρχική τιμή του y
    cout << "Αρχική τιμή y = " << y << endl;
    
    // Πρότερη προσαύξηση (pre-increment): πρώτα αυξάνει, μετά χρησιμοποιεί
    cout << "Πρότερη προσαύξηση ++y: " << ++y << endl;
    cout << "Τιμή y μετά: " << y << endl;
    
    cout << endl;
    
    // Μείωση (decrement)
    int z = 10;
    cout << "Αρχική τιμή z = " << z << endl;
    // Χρησιμοποίησε z-- για ύστερη μείωση
    cout << "Ύστερη μείωση z--: " << z-- << endl;
    cout << "Τιμή z μετά: " << z << endl;
    
    return 0;
}
