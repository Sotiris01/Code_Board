# ΑΣΚΗΣΗ: Συμπλήρωση — Insertion Sort
#
# Ο παρακάτω κώδικας υλοποιεί Insertion Sort
# αλλά λείπουν κάποια κομμάτια.
# Συμπλήρωσε τα κενά (___).
#
# def insertionSort(array):
#     for i in range(___, len(array)):
#         value = array[___]
#         j = i
#         while j > ___ and array[j - 1] > ___:
#             array[___] = array[j - 1]
#             j = j - ___
#         array[___] = value
#
# Αφού συμπληρώσεις, δοκίμασε με:
#   data = [42, 17, 8, 33, 5, 29]
#   insertionSort(data)
#   print(data) → [5, 8, 17, 29, 33, 42]

# Γράψε τον κώδικά σου εδώ

