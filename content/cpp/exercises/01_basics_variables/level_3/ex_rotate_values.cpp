// Άσκηση: Περιστροφή Τιμών 3 Μεταβλητών (Άσκηση 2 από PDF)
// Μετακίνησε τις τιμές: a->b, b->c, c->a

#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    
    // Διάβασε τις τιμές για τις μεταβλητές
    cout << "Δώσε την τιμή του a: ";
    cin >> a;
    cout << "Δώσε την τιμή του b: ";
    cin >> b;
    cout << "Δώσε την τιμή του c: ";
    cin >> c;
    
    cout << "\nΑρχικές τιμές: a=" << a << " b=" << b << " c=" << c << endl;
    
    // TODO: Δήλωσε μια βοηθητική μεταβλητή temp για να αποθηκεύσεις προσωρινά μια τιμή
    int __________;
    
    // TODO: Αποθήκευσε την τιμή του c στην temp
    temp = __________;
    
    // TODO: Βάλε στο c την τιμή του b
    c = __________;
    
    // TODO: Βάλε στο b την τιμή του a
    b = __________;
    
    // TODO: Βάλε στο a την αρχική τιμή του c (που είναι στην temp)
    a = __________;
    
    cout << "Μετά την περιστροφή: a=" << a << " b=" << b << " c=" << c << endl;
    
    // Παράδειγμα: αν a=2, b=8, c=5 -> μετά: a=5, b=2, c=8
    
    return 0;
}
