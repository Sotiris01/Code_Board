// Άσκηση: Περιστροφή Τιμών 3 Μεταβλητών (Άσκηση 2 από PDF)
// Μετακίνησε τις τιμές: a->b, b->c, c->a

#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    
    // Διάβασε τις τιμές για τις μεταβλητές
    cout << "Δώσε την τιμή του a: ";
    cin >> a;
    cout << "Δώσε την τιμή του b: ";
    cin >> b;
    cout << "Δώσε την τιμή του c: ";
    cin >> c;
    
    cout << "\nΑρχικές τιμές: a=" << a << " b=" << b << " c=" << c << endl;
    
    // Δήλωσε μια βοηθητική μεταβλητή temp για να αποθηκεύσεις προσωρινά μια τιμή
    int temp;
    
    // Αποθήκευσε την τιμή του c στην temp
    temp = c;
    
    // Βάλε στο c την τιμή του b
    c = b;
    
    // Βάλε στο b την τιμή του a
    b = a;
    
    // Βάλε στο a την αρχική τιμή του c (που είναι στην temp)
    a = temp;
    
    cout << "Μετά την περιστροφή: a=" << a << " b=" << b << " c=" << c << endl;
    
    // Παράδειγμα: αν a=2, b=8, c=5 -> μετά: a=5, b=2, c=8
    
    return 0;
}
