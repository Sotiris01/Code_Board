/*
 * Άσκηση: Εύρεση Μέγιστου και Ελάχιστου με Θέσεις
 * Επίπεδο: 2
 * 
 * Περιγραφή:
 * Διάβασε 8 αριθμούς από τον χρήστη.
 * Βρες τον μέγιστο και τον ελάχιστο,
 * καθώς και σε ποια θέση (1-8) βρέθηκε ο καθένας.
 * 
 * Παράδειγμα:
 * Είσοδος: 5, 12, 3, 8, 15, 2, 7, 10
 * Έξοδος: 
 *   Μέγιστο: 15 (θέση 5)
 *   Ελάχιστο: 2 (θέση 6)
 */

#include <iostream>
using namespace std;

int main() {
    int number;
    int max, min;
    int maxPos, minPos;
    
    cout << "Δώσε 8 αριθμούς:" << endl;
    
    // TODO: Γράψε ένα for loop από 1 μέχρι 8
    // 
    // ΒΗΜΑ 1: Για τον πρώτο αριθμό (i == 1):
    //   - Θέσε max = min = number
    //   - Θέσε maxPos = minPos = 1
    //
    // ΒΗΜΑ 2: Για τους υπόλοιπους (i > 1):
    //   - Αν number > max:
    //       * max = number
    //       * maxPos = i
    //   - Αν number < min:
    //       * min = number
    //       * minPos = i
    
    
    
    cout << "\n=== ΑΠΟΤΕΛΕΣΜΑΤΑ ===" << endl;
    cout << "Μέγιστο: " << max << " (θέση " << maxPos << ")" << endl;
    cout << "Ελάχιστο: " << min << " (θέση " << minPos << ")" << endl;
    
    return 0;
}
